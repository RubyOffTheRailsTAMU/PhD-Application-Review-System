<!-- app/views/tags/_tags.html.erb -->
<% if tags.any? %>
  <% tags.each do |tag| %>
    <%= tag.name %>
    <!-- Display other tag details as needed -->
  <% end %>
<% else %>
  <p>No tags found for this CAS ID.</p>
<% end %>
